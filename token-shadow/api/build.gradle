description = 'API for persistent oauth shadowing'

apply plugin: 'maven'
version = '0.1-SNAPSHOT'


dependencies {
    compile(
            [
                    project(':auth-core'),
            ]
    )
}